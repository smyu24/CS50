#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int s = get_int("Input starting size: ");
    do
    {


        printf("The number is %i\n", );
    }
    while(s > 0);
    // TODO: Prompt for start size

    // TODO: Prompt for end size

    // TODO: Calculate number of years until we reach threshold

    // TODO: Print number of years
}